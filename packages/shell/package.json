{
  "name": "@anticipy/shell",
  "version": "0.1.0",
  "private": true,
  "description": "Anticipy desktop shell — chat surface + approval cards + inspector panels (Electron + React).",
  "type": "module",
  "main": "dist-electron/main.cjs",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build && node scripts/build-electron.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"(shell has no automated tests; core scenario fixtures are the contract)\"",
    "start": "electron ."
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@anticipy/core": "workspace:*",
    "@types/react": "^18.3.4",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "electron": "^31.4.0",
    "esbuild": "^0.21.5",
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
